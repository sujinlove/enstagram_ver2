<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.enstagram.mapper.EnstaMapper">

	<select id="getAccountList" resultType="com.enstagram.model.EnstaAccount">
		SELECT * FROM ensta_account
	</select>

	<insert id="createAccount" parameterType="com.enstagram.model.EnstaAccount">
		INSERT INTO ensta_account(accnt_num, profile, id, name, email, passwd, role) VALUES(#{accnt_num}, #{profile}, #{id}, #{name}, #{email}, #{passwd}, #{role}) 
	</insert>

	<select id="getAccount" resultType="com.enstagram.model.EnstaAccount">
		SELECT * FROM ensta_account WHERE id=#{id}
	</select>
	
	<!-- Ensta Feed -->
	<select id="getFeedList" resultType="com.enstagram.model.EnstaFeed">
		SELECT * FROM ensta_feed
	</select>

	<insert id="createFeed" parameterType="com.enstagram.model.EnstaFeed">
		INSERT INTO ensta_feed (feed_num, file_name, description, heart, regdate) VALUES (#{feed_num}, #{file_name}, #{description}, #{heart}, NOW()) 
	</insert>

	<select id="getFeed" resultType="com.enstagram.model.EnstaFeed">
		SELECT * FROM ensta_feed WHERE feed_num = #{feed_num}
	</select>

	<!-- Ensta Reply -->
<!-- 	<select id="getReplyList" resultType="com.enstagram.model.EnstaReply">
		SELECT * FROM ensta_reply
	</select>

	<insert id="createReply" parameterType="com.enstagram.model.EnstaReply">
		INSERT INTO ensta_reply (reply_num, feed_num, content, date) VALUES (#{reply_num}, #{feed_num}, #{content}, NOW()) 
	</insert>
 -->
</mapper>